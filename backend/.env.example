# MAGI backend secrets (server-side only — NEVER prefix with EXPO_PUBLIC_)
# Copy to `backend/.env` and fill in. `.env` is gitignored.

HONCHO_API_KEY=hch-v3-vt5girmidqiqr318nst51d76v709lesl30bu30pp7o2203w41ky1nrwsnaov4k95
HONCHO_WORKSPACE_ID=MAGI

# Add other server secrets here as the backend grows:
# OPENAI_API_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
